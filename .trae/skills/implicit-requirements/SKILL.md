---
name: "implicit-requirements"
description: "设计文档驱动的隐含需求推导。从已有设计文档反向推导用户没说出口的前提条件，防止实现时遗漏关键约束�?
---

# Implicit Requirements Derivation

## 定位

�?Skill 提供从设计文档反向推导隐含需求的方法论。核心思想：用户说 A，但已有设计文档中定义了 B/C/D 的约束，新功能必须遵循这些约束——即使用户没提�?
## 何时调用

- Plan 阶段：用户提出新功能需求后，在�?routing.md 之前
- 需求涉及已有系统的扩展�?- 用户�?做一�?XXX"�?XXX 可能与已有设计冲突时

## 检索步�?
### Step 1：搜索设计文�?
```bash
# Glob 搜索设计文档目录
Glob: Docs/<project>/specs/*.md
Glob: Docs/<project>/plans/*.md

# Grep 搜索与需求关键词相关的文�?Grep -i "<�ؼ���1>|<�ؼ���2>" Docs/<project>/ --glob "*.md"
```

### Step 2：读取关键章�?
从搜索结果中找到与当前需求最相关�?2-4 篇，读取关键章节（不全文加载）�?
### Step 3：执行隐含需求推�?
对每�?P0/P1 级设计文档，逐条过推导规则表�?
## 推导规则�?
| 设计文档声明�?| 用户没提但必然需要的 | 优先�?|
|--------------|-------------------|:---:|
| 某个数据模型/类型体系 | 新模块必须复用该类型，不能自创一�?| P0 |
| 某个管道/管线（如 combat pipeline�?| 新模块必须走完整管线，不能截断或简�?| P0 |
| 某个校验规则（如力量门槛�?| 新模块的 UI 必须展示校验结果，不能静默跳�?| P1 |
| 某个状态隔离约束（如沙盒不等于存档�?| 新模块的状态管理方案必须与此约束一�?| P0 |
| 某个 UI/交互约定 | 新模块的视觉效果必须一致（如颜色、按钮样式） | P1 |
| 某文档提�?A 依赖 B，但用户只提�?A | B 可能也需要一并处理或至少提醒用户 | P1 |

## 输出格式

推导结果写入 analysis.md �?隐含需�?章节�?
```text
隐含需求（从设计文档推导，用户未提及）
├── [P0] 必须复用 equipment-types 品质体系 �?沙盒装备面板需展示品质/耐久/附魔�?├── [P0] 必须走完�?resolveFullAttack 12 步管�?�?战斗日志暴露每一步结�?├── [P1] 力量门槛校验 �?装备赋予时弹警告而非静默忽略
├── [P1] d20 命中判定暴露 �?日志显示命中判定细节
└── [P2] 沙盒状态用独立状态管理，不注册到主线 �?参�?spec Scenario 13
```

同时输出设计文档摘要�?
```text
已有设计文档索引（来源：Docs/superpowers/�?├── <文档1> �?相关章节�?章节�? �?关键约束�?1-2 �?
├── <文档2> �?相关章节�?章节�? �?关键约束�?1-2 �?
└── <文档3> �?相关章节�?章节�? �?关键约束�?1-2 �?
本次设计将遵循以上约束，如有冲突必须向用户提出�?```

## 硬门禁（两条，缺一不可�?
**门禁 1**：如果存在相关设计文档但 analysis.md 中未引用 �?禁止进入依赖链推导�?
**门禁 2**：如果设计文档中定义了模�?管线/校验/UI 约定，但 analysis.md 中隐含需求章节未覆盖 �?禁止进入依赖链推导�?
违反任一门禁 �?回到检索步骤，补全后再继续�?
## 推导示例

用户说："做一个沙盒战斗测试模�?

读装备设计文�?�?装备有品质体系、附魔槽、力量门槛：
- [P0] 沙盒必须复用装备品质体系，不能自己另搞一�?- [P0] 赋予装备时需检查力量门槛，不能跳过
- [P1] 附魔�?UI 是否需要？�?设计文档有但用户没提 �?需确认

读战斗设计文�?�?12 步管线、格�?招架/碾压/暴击�?- [P0] 沙盒战斗不能简化管线，必须走完�?resolveFullAttack
- [P0] 战斗日志要暴露中间步骤（格挡量、碾压量），否则测试无意�?- [P1] 需要暴�?d20 命中判定细节，方便开发者调�?
读存档设计文�?�?沙盒数据需隔离�?- [P0] 不能调用 save-repository
- [P0] 需要独立的沙盒状态管理（不注册到主线状态管道）

## 常见设计文档目录

| 项目 | 设计文档位置 | 命名模式 |
|------|------------|---------|
| AIRPG Web | `Docs/superpowers/specs/` | `YYYY-MM-DD-airpgweb-<主题>-design.md` |
| RTS (UE5) | `Docs/superpowers/specs/` | `YYYY-MM-DD-rts-<主题>-design.md` |
| CharacterDesignTool | `Docs/superpowers/specs/` | `YYYY-MM-DD-characterdesigntool-<主题>-design.md` |

## 禁止事项

- 不跳过设计文档检索直接推�?- 不在硬门禁未通过时进入实�?- 不把"可能有用"的约束全部标�?P0（区分优先级�?- 不替用户决定隐含需求中的待确认项（标出并询问）
